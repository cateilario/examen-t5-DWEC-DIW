













































        const showDetails = () => {
            document.form.actors.forEach(actor =>
                actor.checked ? alert(actor.value) : null
            )
        }

